# An infinite-sites like mainland-island model: each mutation is unique and
# happens at a different locus.
# We maintain the architecture/genome distinction, but should only track who
# has which mutation
